[project]
name = "ragme"
version = "0.1.0"
description = "RAGme project. Retrieval-Augmented Generation for starred GitHub repositories, Notion pages and bookmarked X links."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lightrag-hku[api]>=1.4.9.10",
]
